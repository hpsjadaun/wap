CREATE TABLE CUSTOMERLDC(ID INT IDENTITY(1,1), NAME VARCHAR (20) NOT NULL , AGE INT NOT NULL,PRIMARY KEY (ID));
CREATE TABLE CUSTOMERLDC(ID INT IDENTITY(1,1), NAME VARCHAR (20) NOT NULL , AGE INT NOT NULL,PRIMARY KEY (ID));